# ARCNODE appliance config vars. Config only — secrets live in env, never here.
site_id: arcnode-appliance
http_port: 80
log_level: INFO

# Ollama model assignments by ROLE (ADR-024). Consumers reference role names,
# staying decoupled from specific versions. Tags match the arcnode Ollama
# endpoint (173.211.12.43) + the analyst agent/mcp cfg.defaults — the airgap
# bundle pulls exactly these. (:cloud models are passthrough, not airgappable.)
ai_models:
  chat: gemma4:26b
  code: qwen2.5:7b-instruct-q4_K_M
  embedder: qwen3-embedding:4b
